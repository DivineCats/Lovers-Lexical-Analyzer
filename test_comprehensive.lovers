boundaries MyNamespace {
    dear globalVar = 100;
    
    avoidant helper() {
        express << "Helper function";
    }
}

const dear MAX = 100;

dear add(dear a, dear b) {
    comeback a + b;
}

avoidant printArray(dear arr[]) {
    express << "Array contents";
}

love main() {
    dear x = 5;
    dear y = 10;
    dearest pi = 3.14159;
    rant message = "Hello, Lovers!";
    status isActive = greenflag;
    dear a, b = 20, c;
    dear result = add(x, y);
    dear arr[] {1, 2, 3};
    status flag = greenflag;
    status equal = greenflag;
    status notEqual = greenflag;
    status less = greenflag;
    status greater = greenflag;
    
    express << result;
    express << periodt;
    
    give >> x;
    overshare(y);
    
    forever (x < 10) {
        express << x;
        x = x + 1;
    } forevermore (x == 10) {
        express << "x is exactly 10";
    } more {
        express << "x is greater than 10";
    }
    
    while (y > 0) {
        y = y - 1;
    }
    
    pursue (isActive) {
        express << "Still active";
    }
    
    for (dear i = 0; i < 5; i ++ ) {
        express << i;
    }
    
    choose (x) {
        phase 1: 
            express << "One";
            breakup;
        phase 2:
            express << "Two";
            breakup;
        bareminimum:
            express << "Default";
            breakup;
    }
    
    x += 5;
    x -= 1;
    x *= 2;
    x /= 2;
    x %= 3;
    
    ++ x;
    -- y;
    x ++;
    
    flag = (x > 5) && (y < 10);
    equal = x == y;
    notEqual = x != y;
    less = x < y;
    greater = x >= y;
    
    comeback 0;
}
